# Add project dependencies here, one per line.
# Pin versions so both of us run the same setup:
numpy==2.1.0
matplotlib==3.9.2
scipy==1.14.1        # optimisation for thrust allocation (linprog / milp)
pytest==8.3.3        # run tests with:  python -m pytest
#   pandas==2.2.2
#
# Install:  pip install -r requirements.txt
# Freeze:   pip freeze > requirements.txt
